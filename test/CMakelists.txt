cmake_minimum_required(VERSION 3.8)

set(This powerwindowstests)

set(Sources
    power_windowstests.cpp
)

add_executable(${This} ${Sources})
target_link_libraries(${This} PUBLIC
    gtest_main
    gmock_main
    power_windows
)

add_test(
    NAME ${This}
    COMMAND ${This}
)
